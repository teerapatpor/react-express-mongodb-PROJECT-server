const postController = require("./post");
const userController = require("./user");
const chatController = require("./chat");

module.exports = {
  postController,
  userController,
  chatController,
};
